# Deixe em branco/comentado para o front detectar automaticamente o backend na mesma
# máquina (funciona tanto em localhost quanto acessando pelo IP na rede local).
# Defina só se o backend estiver em outro endereço (ex: produção).
# VITE_API_URL=http://localhost:3333/api
